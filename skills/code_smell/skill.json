{
  "name": "code_smell",
  "description": "Detect code smells in Python: long functions, deep nesting, too many parameters, large files, god classes",
  "input_schema": {
    "type": "object",
    "properties": {
      "code": {
        "type": "string",
        "description": "Python source code to analyze"
      },
      "file_path": {
        "type": "string",
        "description": "Path to a Python file (alternative to code)"
      },
      "thresholds": {
        "type": "object",
        "description": "Custom thresholds: max_function_lines, max_params, max_nesting, max_file_lines, max_complexity, max_methods",
        "default": {}
      }
    }
  },
  "execution": {
    "type": "python",
    "entry": "handler.py"
  }
}
