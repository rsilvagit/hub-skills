{
  "name": "security_scan",
  "description": "Scan Python code for security issues: hardcoded secrets, SQL injection, command injection, insecure functions, debug leftovers",
  "input_schema": {
    "type": "object",
    "properties": {
      "code": {
        "type": "string",
        "description": "Python source code to scan"
      },
      "file_path": {
        "type": "string",
        "description": "Path to a Python file (alternative to code)"
      }
    }
  },
  "execution": {
    "type": "python",
    "entry": "handler.py"
  }
}
